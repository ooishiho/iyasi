<div class="container">
  <div class="row">
    <div class="">
     <h1 class="text-center">投稿詳細</h1>
     <div class="form-group">
        <% image_tag @post_item.get_item_image(300,600) %> 
     </div>
     <div class="form-group">
        <%= link_to @user.name,admin_customer_path(@user.id) %>
     </div>
     <div class="form-group">
       <%= @post_item.introduction %> 
     </div>
    <div class="form-group">
      総合評価
    </div>
    <div class="form-group">
     <%= link_to '投稿を削除する',admin_post_item_path, method: :delete, data: {confirm: "本当に削除しますか？"},class: 'btn-btn-denger' %>
    </div>
    </div>
  </div>
</div>